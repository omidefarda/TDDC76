#include "list.h"
#include <string>
#include <iostream>
using namespace std;

int main ()
{
  string namn;
  string textfil;
  list namnlista;
  list::node* ptr;
  ifstream infil;
  cin >> texfil;
  if (
  while(cin >> namn)
    {
      ptr = namnlista.name(namn);
      namnlista.addnode(ptr);
    
    } 
}
